# Raw data — large Excel files, not tracked in git
# Drop updated files here and delete the relevant data/processed/ cache to refresh
data/raw/

# Processed cache files — regenerated by running the notebook
# The HTML map IS tracked so it can be served via GitHub Pages
data/processed/ma_bg_geom_only.geojson
data/processed/ma_bg_population.csv
data/processed/block_groups_with_coverage.geojson
data/processed/stations_combined.geojson
data/processed/coverage_buffer.geojson

# Local environment — API keys, tokens; never commit
.env

# Claude Code local settings
.claude/

# Python
__pycache__/
*.pyc
.DS_Store
.ipynb_checkpoints/
